import styled from '@emotion/styled'

const ServiceItemDescription = styled.span`
  font-size: 16px;
  line-height: 23px;
  letter-spacing: 1.1px;
  color: ${({ theme }) => theme.palette.gray[100]};
`

export default ServiceItemDescription

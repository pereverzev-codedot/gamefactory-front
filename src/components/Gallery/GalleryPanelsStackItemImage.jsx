import styled from '@emotion/styled'
import Image from '../Image'

const GalleryPanelsStackItemImage = styled(Image)`
  width: 100%;
  display: block;
  cursor: pointer;
`

export default GalleryPanelsStackItemImage
